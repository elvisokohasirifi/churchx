@extends(backpack_view('blank'))
@section('content')
<div class="row g-3"><div class="col-12 col-lg-4"><div class="card"><div class="card-header"><h3 class="card-title">New Account Transfer</h3></div><form method="POST" action="{{ route('admin.transfers.store') }}">@csrf<div class="card-body">@foreach(['from_account_id'=>'From account','to_account_id'=>'To account'] as $name=>$label)<label class="form-label">{{ $label }}</label><select class="form-select mb-3" name="{{ $name }}" required>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->name }} ({{ $account->currency }})</option>@endforeach</select>@endforeach<label class="form-label">Amount</label><input class="form-control mb-3" type="number" step=".0001" min=".0001" name="amount" required><div class="row"><div class="col"><label class="form-label">Currency</label><input class="form-control text-uppercase" name="currency" value="{{ $currency }}" maxlength="3" required></div><div class="col"><label class="form-label">Date</label><input class="form-control" type="date" name="date" value="{{ today()->toDateString() }}" required></div></div></div><div class="card-footer text-end"><button class="btn btn-primary">Submit</button></div></form></div></div><div class="col-12 col-lg-8"><div class="card"><div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Amount</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($transfers as $transfer)<tr><td>{{ $transfer->date->format('M j, Y') }}</td><td>{{ $transfer->currency }} {{ number_format((float)$transfer->amount,2) }}</td><td>{{ str($transfer->status->value)->headline() }}</td><td>@if($transfer->status->value==='pending_approval')<form method="POST" action="{{ route('admin.transfers.approve',$transfer) }}">@csrf<button class="btn btn-sm btn-outline-primary">Approve</button></form>@elseif($transfer->status->value==='approved')<form method="POST" action="{{ route('admin.transfers.complete',$transfer) }}">@csrf<button class="btn btn-sm btn-success">Complete</button></form>@endif</td></tr>@empty<tr><td colspan="4">No transfers yet.</td></tr>@endforelse</tbody></table></div></div>{{ $transfers->links() }}</div></div>
@endsection
