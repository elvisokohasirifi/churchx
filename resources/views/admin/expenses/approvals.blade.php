@extends(backpack_view('blank'))
@section('content')
<h2 class="mb-3">Expense Approval Queue</h2><div class="row g-3">@forelse($expenses as $expense)<div class="col-12"><div class="card"><div class="card-body"><div class="d-flex justify-content-between"><div><h3 class="card-title">{{ $expense->recipient_name }}</h3><p>{{ $expense->description }}</p></div><strong>{{ $expense->currency }} {{ number_format((float)$expense->amount,2) }}</strong></div><form method="POST" action="{{ route('admin.expenses.decide',$expense) }}" class="row g-2">@csrf<div class="col-md"><textarea class="form-control" name="comments" placeholder="Decision notes"></textarea></div><div class="col-md-auto"><button class="btn btn-success" name="decision" value="approved">Approve</button> <button class="btn btn-outline-danger" name="decision" value="rejected">Reject</button></div></form></div></div></div>@empty<div class="col-12"><div class="alert alert-info">No expenses are waiting for approval.</div></div>@endforelse</div>{{ $expenses->links() }}
@endsection
