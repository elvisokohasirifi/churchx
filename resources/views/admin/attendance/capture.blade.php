@extends(backpack_view('blank'))
@section('content')
<div class="d-flex justify-content-end mb-3"><a class="btn btn-outline-primary" href="{{ route('admin.attendance.register') }}">Use attendance register</a></div><div class="card"><div class="card-header"><h3 class="card-title">Attendance Capture</h3></div><form method="POST" action="{{ route('admin.attendance.store') }}">@csrf
<div class="card-body"><div class="row g-3"><div class="col-12 col-md-6"><label class="form-label">Service</label><select name="service_id" class="form-select" required>@foreach($services as $service)<option value="{{ $service->id }}">{{ $service->name }} — {{ $service->date->format('M j, Y') }}</option>@endforeach</select></div><div class="col-12 col-md-6"><label class="form-label">Branch</label><select name="branch_id" class="form-select" required>@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->name }}</option>@endforeach</select></div>
@foreach(['total_male'=>'Adult male','total_female'=>'Adult female','total_children'=>'Children','total_members'=>'Members','total_visitors'=>'Visitors'] as $name=>$label)<div class="col-6 col-md"><label class="form-label">{{ $label }}</label><input class="form-control attendance-number" type="number" min="0" value="0" name="{{ $name }}" required></div>@endforeach</div><div class="alert alert-info mt-3 mb-0">Total attendance: <strong id="attendance-total">0</strong>. Member and visitor figures classify the same people and are not added again.</div></div><div class="card-footer text-end"><button class="btn btn-primary">Save summary</button></div></form></div>
@push('after_scripts')<script>document.querySelectorAll('.attendance-number').forEach(i=>i.addEventListener('input',()=>{const a=[...document.querySelectorAll('.attendance-number')].slice(0,3);document.getElementById('attendance-total').textContent=a.reduce((t,i)=>t+(Number(i.value)||0),0)}));</script>@endpush
@endsection
