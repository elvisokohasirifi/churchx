@extends(backpack_view('blank'))
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><h2>Offering Collections</h2><a class="btn btn-primary" href="{{ route('admin.offerings.create') }}">Capture offering</a></div>
<div class="card"><div class="table-responsive"><table class="table table-vcenter"><thead><tr><th>Date</th><th>Status</th><th>Items</th><th>Actions</th></tr></thead><tbody>@forelse($collections as $collection)<tr><td>{{ $collection->date->format('M j, Y') }}</td><td><span class="badge bg-blue-lt">{{ str($collection->status->value)->headline() }}</span></td><td>{{ $collection->items_count }}</td><td class="d-flex gap-2">@if($collection->status->value==='counted')<form method="POST" action="{{ route('admin.offerings.verify',$collection) }}">@csrf<button class="btn btn-sm btn-outline-primary">Verify</button></form>@endif @if($collection->status->value==='verified')<form method="POST" action="{{ route('admin.offerings.post',$collection) }}" onsubmit="return confirm('Post this offering to income? This action is auditable.')">@csrf<button class="btn btn-sm btn-success">Post</button></form>@endif</td></tr>@empty<tr><td colspan="4" class="text-center text-muted p-4">No offering collections yet.</td></tr>@endforelse</tbody></table></div></div>{{ $collections->links() }}
@endsection
